{
    "scripts": {
        "pretest": "cd .. && ts-node --transpile-only --ignore=false test/pretest.ts",
        "test": "cd .. && ETHEREUM_URL=https://rpc.ankr.com/eth ts-node --transpile-only --ignore=false test/test.ts"
    },
    "dependencies": {
        "azle": "github:demergent-labs/azle"
    },
    "devDependencies": {
        "@dfinity/agent": "0.14.1",
        "ts-node": "10.7.0",
        "typescript": "4.6.3"
    }
}
