service : (principal) -> {
  argument : () -> (principal) query;
  id : () -> (principal);
  id_quick : () -> (principal) query;
  installer : () -> (principal) query;
  whoami : () -> (principal);
}
