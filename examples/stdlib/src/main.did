service : () -> {
  test_base64 : () -> (vec nat8) query;
  test_collections : () -> (text) query;
  test_datetime : () -> (text) query;
  test_itertools : () -> (vec text) query;
  test_json : () -> (text) query;
  test_random : () -> (float64);
  test_string : () -> (text) query;
  test_urllib : () -> (text) query;
  test_uuid : () -> (text);
}