{
    "scripts": {
        "pretest": "cd .. && KYBRA_REBUILD=true ts-node --transpile-only --ignore=false test/pretest.ts",
        "test": "cd .. && KYBRA_REBUILD=true ts-node --transpile-only --ignore=false test/test.ts"
    },
    "devDependencies": {
        "@dfinity/agent": "0.11.1",
        "azle": "github:demergent-labs/azle",
        "ts-node": "10.7.0",
        "typescript": "4.6.3"
    }
}
